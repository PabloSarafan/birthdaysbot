# 🔄 Восстановление старой базы данных

## Способ 1: Через SSH к серверу CapRover

### Шаг 1: Подключитесь к серверу

```bash
ssh root@ваш-сервер
```

### Шаг 2: Найдите контейнер бота

```bash
docker ps | grep birthdaybot
```

Скопируйте CONTAINER ID (первая колонка)

### Шаг 3: Зайдите в контейнер

```bash
docker exec -it <CONTAINER_ID> /bin/bash
```

### Шаг 4: Проверьте, есть ли старая база

```bash
# Проверяем в корне приложения
ls -la /app/*.db

# Проверяем в новой папке
ls -la /app/data/*.db
```

### Шаг 5а: Если база есть в /app/birthdays.db - скопируйте ее

```bash
# Если база в /app/, копируем в /app/data/
cp /app/birthdays.db /app/data/birthdays.db

# Проверяем
ls -la /app/data/birthdays.db
```

### Шаг 5б: Если базы нет - проверьте volumes

```bash
# Выйдите из контейнера
exit

# Найдите volume
docker volume ls | grep birthdaybot

# Посмотрите содержимое volume
docker volume inspect birthdaybot-data
```

### Шаг 6: Перезапустите приложение в CapRover

В CapRover Dashboard нажмите "Save & Update" чтобы перезапустить приложение.

---

## Способ 2: Если база была потеряна - ручное восстановление

Если старая база данных потеряна, придется добавить записи заново. Но теперь они будут сохраняться!

---

## Способ 3: Экспорт с локальной базы (если вы тестировали локально)

Если у вас есть записи в локальной базе данных:

### На вашем Mac:

```bash
cd /Users/psarafannikov/birthdaysbot

# Проверьте локальную базу
sqlite3 birthdays.db "SELECT * FROM birthdays;"

# Экспортируйте в SQL
sqlite3 birthdays.db .dump > backup.sql
```

### Загрузите на сервер:

```bash
# Скопируйте файл на сервер
scp birthdays.db root@ваш-сервер:/tmp/

# Подключитесь к серверу
ssh root@ваш-сервер

# Найдите контейнер
docker ps | grep birthdaybot

# Скопируйте базу в контейнер
docker cp /tmp/birthdays.db <CONTAINER_ID>:/app/data/birthdays.db

# Перезапустите приложение в CapRover
```

---

## Проверка после восстановления

1. Откройте бота в Telegram
2. Отправьте `/list`
3. Все записи должны появиться! ✅

---

## Если ничего не помогло

К сожалению, если контейнер был полностью пересоздан и persistent storage не был настроен, данные могли быть потеряны безвозвратно. 

Но теперь с настроенным persistent storage все новые записи будут сохраняться навсегда! 🎉

